# Generated by .brand/gen_repo_build.py — regenerate rather than hand-edit.
#
# Builds this app's images from the source in this directory. Used by ./build.sh;
# the server never needs this file, only compose.yaml.

name: techyst-16-openhands
services:
  openhands:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-openhands-openhands:${TAG:-latest}
    build:
      context: .
      dockerfile: ./docker/Dockerfile
      cache_from:
      - ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-openhands-openhands:latest
      args:
        AGENT_SERVER_IMAGE: ghcr.io/openhands/agent-server:1.28.1-python
        AUTOMATION_VERSION: 1.10.0
