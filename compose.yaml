# Generated by .brand/gen_repo_compose.py — regenerate rather than hand-edit.
#
#   infisical export --projectId techyst-16-openhands --env prod --format dotenv > .env
#   docker compose up -d
#
# Images come from ghcr.io; `docker login ghcr.io` first. Databases, cache,
# queue and object storage are all external, so nothing here holds state and
# `docker compose down` is safe.

name: techyst-16-openhands
services:
  openhands:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-openhands-openhands:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    ports:
    - 3070:8000
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
